# 抖音自动续火花

## 仓库描述

贴到 GitHub 仓库主页 About 那一栏：

```
给抖音好友自动续火花的开源工具。Playwright 驱动真实 Chromium 操作网页版，登录态存本地；Linux 服务端一条命令装成 systemd 服务，Windows 端解压双击就能用。
```

嫌长可以用这句短的：

```
抖音好友续火花自动化，浏览器自动化路线，Linux 服务端加 Windows 桌面端。
```

描述栏上限 350 字，上面两句在常规视图里都能完整显示。

## 项目简介

抖音的「火花」是私信里的连续互动记录，好友之间互相发消息，天数一天天往上加，断一天就清零。想让数字不断，就得每天记得发一遍。这个程序把这件事交给机器：到点给指定好友发消息，发完退出，第二天再来。

做法是浏览器自动化。Playwright 驱动一个真实的 Chromium 打开抖音网页版，扫码登录一次，把登录态存到本机的文件里，之后都复用这份登录态。不调私有接口，不改客户端，也不用抓包。

代码分两个端。服务器端跑在 Linux 上，FastAPI 加无头 Playwright，一台机器常挂，浏览器打开网页界面就能看任务和日志；Windows 端是 PySide6 写的桌面程序，引擎内置在本机，不用另外开服务器。两端的 `app/` 和 `server/` 是同一份文件，逐字节相同，不会出现两套逻辑。

项目是个人开发者的业余作品，跟抖音、字节跳动没有任何关系，也没有拿到过官方授权或者背书。发布在非商业使用许可协议下，自己学习和给自己账号续火花可以，拿去做生意不行。

## 功能

- 多账号管理，扫码登录，登录态存本机，文件权限收到 0600
- 每个账号可以配多条文案，发送时随机挑一条
- 定时任务带随机抖动，不会整点齐刷刷发
- 拟人停顿：好友之间 30-90 秒，小概率拉长到 2-5 分钟，单条消息之间有下限
- 浏览器指纹一致性处理，降低被识别成自动化的概率
- 日志、任务结果和截图里的好友昵称统一替换成「好友01」「好友02」
- 可选的钉钉机器人通知，任务跑完推一条结果；不填就没有任何外部请求
- 状态监控页，实时日志、历史记录、任务结果都能看

## 两个端

| 项目 | 服务器端 | Windows 端 |
| --- | --- | --- |
| 运行环境 | Linux，Python 3.9 以上 | Windows，解压压缩包 |
| 技术栈 | FastAPI + 无头 Playwright | PySide6 + 内置引擎 |
| 启动方式 | `bash deploy.sh --src /root/lin端` | 双击 `DouyinFireConsole.exe` |
| 常驻方式 | systemd 单元 `douyin-fire`，开机自启，崩溃自动重启 | 关掉窗口即退出 |
| 默认端口 | 8765 | 本地引擎 18765-18804 |
| 界面 | 浏览器打开网页界面 | 本机桌面窗口 |

## 快速开始

服务器端要 Python 3.9 以上和一个能跑 Chromium 的环境：

```bash
bash deploy.sh --src /root/lin端
```

系统级安装前面加 `sudo`，不想用 root 就加 `--user`。脚本会装系统依赖、建 venv、下载 Chromium、生成 `config.json`（Token 随机生成）、注册 systemd 服务，最后打印内网和公网地址、端口、API Token，以及 Win 端怎么配。

Windows 端解压 `抖音续火花_成品版_打开即用.zip`，双击 `DouyinFireConsole.exe`。

两边都走一遍的详细步骤在 `使用说明.txt`。

## 风险

抖音的风控能识别自动化行为，限流、降权、要求重新验证、封号都有可能。程序里做了随机延时和浏览器指纹处理，只能降低概率，给不了任何保证。

自己学习、研究、维护自己账号的日常消息没问题。骚扰、垃圾信息、批量操作别人账号不行。条款细节和合规责任写在 `免责声明.txt` 里。

## 协议

非商业使用许可，见 `LICENSE`。个人学习、技术研究、给账号续火花都可以；出售、代运营、接商业项目、在公司经营活动中使用都不行。要用在商业场景，先找作者拿书面授权。

## 建议的话题标签

GitHub 的 Topics 填这几个：`douyin`、`playwright`、`browser-automation`、`fastapi`、`pyside6`、`python`、`scheduled-tasks`、`non-commercial`
